# Reference implementation

`gateway.example.ts` is a **skeleton, not a working layer**. It shows the shape
of each method and where the real work goes. Nothing imports it, and it will not
run as-is.

Copy it to `src/ai/gateway.ts`, implement the marked sections, and register it
from `src/server.ts`.
